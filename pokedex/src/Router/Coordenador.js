export  const irParaListaDePokemons = (history) => {
    history.push("/")
}

export  const detalhePokemon = (history, name) => {
     history.push(`/pokemon/${name}`)
}

export  const paraPokedex = (history) => {
    history.push("/pokedex")
 
}





